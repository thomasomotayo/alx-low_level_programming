#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * _atoi - Entry point
 * @s: input
 * Return: Always 0 (Success)
 */

int _atoi(char *s)
{
	 int i;
	 char *s;
	 s = " -9885";
	 i = atoi(s); 
	_putchar("i = %d\n",i);

	return (0);
}
