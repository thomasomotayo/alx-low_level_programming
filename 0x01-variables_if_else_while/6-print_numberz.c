#include <stdio.h>

/**
* main - Entry point
*
* Return: returns 0 when everything works fine
*
*/

int main(void)
{
i	int n;

	for (n = 48; n < 58; ++n)
	{
		putchar(n);
	}
	putchar(10);
	return (0);
}
