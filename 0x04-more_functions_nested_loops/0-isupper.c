#include "main.h"
/**
 * _isuper - function that check the uppercase character
 * 0c: input
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _isupper(int c);
{
if((c >= 'A') && (c <= 'Z'))
return (1);
else
retuen (0)
}
